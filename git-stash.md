# git stash
`git stash` — это как «пауза» в разработке. Когда ты такой:

> "Блин, я сейчас ковыряю одну штуку, но срочно надо переключиться на другую… И коммитить эту кашу нельзя!" 😵‍💫

Вот тут и появляется **спаситель**:

---

## 🧠 Что делает `git stash`?

🧳 **Временно убирает все твои незакоммиченные изменения**, чтобы ты мог спокойно переключиться на другую ветку.
Git сохраняет их «в карман» и возвращает рабочую папку к чистому состоянию (как после `git checkout`).

---

## 📦 Как использовать?

### 1. **Спрятать изменения:**

```bash
git stash
```

👉 Убирает все изменения (и в файлах, и в индексе)
Рабочая директория становится чистой.

---

### 2. **Вернуть изменения обратно:**

```bash
git stash pop
```

👉 Возвращает то, что ты спрятал, **и удаляет** этот stash.

или:

```bash
git stash apply
```

👉 Возвращает, **но не удаляет** из хранилища.

---

### 3. **Посмотреть, что ты спрятал:**

```bash
git stash list
```

📋 Покажет список сохранённых stash'ей, например:

```
stash@{0}: WIP on main: 2b8f4ab добавил черновик
stash@{1}: WIP on main: 0c7e2d1 эксперимент с формами
```

---

### 4. **Вернуть конкретный stash:**

```bash
git stash pop stash@{1}
```

---

### 5. **Удалить stash:**

```bash
git stash drop stash@{0}
```

Или всё сразу:

```bash
git stash clear
```

---

## 🧠 Для чего полезно?

| Ситуация                                                 | Что делать      |
| -------------------------------------------------------- | --------------- |
| Нужно срочно сменить ветку, но есть несохранённые правки | `git stash`     |
| Хочешь быстро спрятать «грязный» код и не коммитить      | `git stash`     |
| Возвращаешься к задаче                                   | `git stash pop` |

---

## ⚠️ Важно

* `git stash` не сохраняет **неотслеживаемые файлы** (т.е. те, что не были добавлены через `git add`)
  👉 Если надо сохранить вообще всё:

```bash
git stash -u  # или --include-untracked
```

---

## 🎮 Пример:

```bash
# Вносишь изменения в index.html
git stash      # Спрятал
git checkout main  # Переключился
git checkout feature  # Назад в свою ветку
git stash pop   # Вернул изменения
```

---
